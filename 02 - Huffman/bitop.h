/**
 * DEF: DEFINE O BIT SELECIONADO COMO 1
 *
 * @param character --- ELEMENTO A SER MODIFICADO
 * @param position --- POSICAO A QUAL SE DESEJA MUDAR O BIT
 * @return INT --- RETORNA O NOVO VALOR
 */
int isBitISet(unsigned char c, int i);
/**
 * DEF: VERIFICA SE O BIT DO ELEMENTO ESTA DEFINIDO COMO 1
 *
 * @param character --- ELEMENTO QUE POSSUE O BIT A SER VERIFICADO
 * @param position --- POSICAO DO BIT A SER VERIFICADO
 * @return INT --- RETORNA VERDADEIRO SE O BIT ESTIVER DEFINIDO COMO 1 NAQUELA POSICAO
 */
int isBitISet(unsigned char c, int i);
